# UO-04 click-through checklist

This checklist maps the local prototype to `ui-overhaul/STATE-MAP.md`. It is evidence for the artifact's coverage only; it is not a claim of participant or production validation.

| Done | State-map path | Rehearsal | Expected observation |
| --- | --- | --- | --- |
| [ ] | H0 → Q1 | Supported home example → Start research | First-run explainer is separate; question remains editable; no run or charge |
| [ ] | H0 → U1 | Unsupported example | Scope notice names supported synthetic data-centre scope; no unrelated evidence |
| [ ] | H1 → Q1 | See returning-user state → Resume draft | Draft card appears; question and cap are retained; landing explanation is not required |
| [ ] | Q1 → S1 | Continue to sources | Named allowed-to-read profiles; helper avoids truth guarantee |
| [ ] | S1 self-loop | Uncheck all profiles | Inline minimum-selection error; Continue is disabled |
| [ ] | S1 → B1 | Select profile → Continue | Selected count persists; budget is next decision |
| [ ] | B1 → V1 | Adjust cap → Continue | Maximum research spend is visibly a cap, separate from wallet and source price |
| [ ] | V1 → V2 → R1 | Open advanced → Approve plan | Readable plan default; advanced fields disclosed; approval is not purchase consent |
| [ ] | R1 → A1 → A2 | Open answer from available evidence | Answer is available without BUY; labels say open/fixture; citations are claim-level |
| [ ] | R1 → P1 → P2 | Review purchase | Recommendation remains optional; one focused exact-quote modal shows source, amount, expiry, mode, after-purchase cap/spent/remaining, terms |
| [ ] | P2 → P1 | Cancel / Escape | No state or spend change; focus returns to invoking action |
| [ ] | P2 → P3 → R1 | Approve exact purchase | Simulated receipt, 1.10 XRP spend, protected source unlocked; no real publisher payment claim |
| [ ] | P1 → P3 | Prototype controls → Expire quote | No charge; Re-quote is the only safe path |
| [ ] | R1 → P3 | Prototype controls → Block by cap | System-owned block reason and Continue without it; open answer remains available |
| [ ] | P3 → L1 | Prototype controls → Unknown payment | Reconcile action; no blind retry or paid/unlocked claim |
| [ ] | P3 → F1 | Prototype controls → Fail access | Settlement/access distinction; protected body stays unavailable; recovery action |
| [ ] | R1 → T1 → R1 | Pause → Resume | Server-state copy, preserved question/cap/receipts, no invented progress |
| [ ] | R1 → L1 | Prototype controls → Simulate reload | Recovery banner says authoritative fixture run restored and preserves safe state |
| [ ] | A2 citation inspection | View citation | Exact accessible open span, evidence mode, and binding reason are shown; close returns to answer |

Responsive review targets: 360, 390, 768, 1024, and 1440 CSS px, plus 320px reflow and 200% text zoom. Confirm no body horizontal overflow, clipped critical action, hidden purchase consent, or third desktop column.
