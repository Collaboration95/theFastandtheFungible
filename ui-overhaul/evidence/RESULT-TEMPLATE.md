# Work-package verification record

- Work ID / title:
- Baseline and tested commit:
- Date / OS / Node / browser:
- Worker / reviewer:
- Files changed:
- Mode: fixture LLM / fixture settlement / isolated run-store path (no secrets)
- User-visible behavior before → after:

## Acceptance results

| Criterion / scenario | PASS / FAIL / NOT RUN | Reproduction or command | Evidence path | Limitation / next action |
| --- | --- | --- | --- | --- |
| Fill each assigned criterion | NOT RUN | | | |

## Rendered review

- Screen states and viewports actually inspected:
- Screenshot paths and before-image IDs:
- Long text / zoom / narrow reflow:
- Keyboard, focus, Escape and restoration:
- Error, retry, cancellation, stale-state behavior:
- Cap, quote, access and citation checks when applicable:
- Console/network errors reviewed:

## Gate decision

ACCEPT / REVISE / BLOCKED, with a concrete reason. Automated pass alone does not
accept UI work. Record anything not tested and its effect on demo readiness.
Do not commit participant identities, keys, runtime stores, or sensitive traces.
